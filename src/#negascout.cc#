#include "algoritmos.hh"

int negascout(state_t n,int alpha,int beta,bool color){

  if(n.terminal()){
    return n.value();
  }

  int b= beta;

  if(color){
    for(int pos=0; pos < DIM; ++pos){
      
    }
  }else{

  }

}